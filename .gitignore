# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout

# 生成的工程可由 project.yml 重新生成,不入库(克隆后执行 xcodegen generate)
MouseTools.xcodeproj

# SwiftPM
.build/
.swiftpm/

# macOS
.DS_Store

# 构建产物:签名后的分发包走 GitHub Release,不入库
*.dmg

# 本地工具 / IDE 配置(含本机用户名、Team ID 等,勿入库)
.claude/
.idea/
